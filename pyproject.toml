[project]
name = "skol"
version = "0.0.1"
dependencies = ["openai==0.26",
	        "click<=8.1.3"]

[project.scripts]
skol = "main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

# [tool.setuptools.packages.find]
# where = ["src"]
# exclude = ["src.tests*"]
